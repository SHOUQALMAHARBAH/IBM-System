@AGENTS.md

# CLAUDE.md — ibms-brain

Workspace instructions. Loaded into every Claude Code session in this repo and in every engineering repo that syncs the brain, once those repos exist.

<!-- The @AGENTS.md line above is an import, not a link. Claude Code reads CLAUDE.md and
     NOT AGENTS.md, so without that import the AGENTS.md file is dead weight for Claude
     while still being read by other agent tools. Keep the import on line 1.
     Target for this file: under 200 lines. Longer files reduce adherence. -->

---

## What's New

| Date | Change | Action required |
|------|--------|-----------------|
| 2026-09-07 | **NEW `meta/context/internal-controls-audit.md`** (Process 56) — filed via `/brain-gap` at `ibms-app` (backlog Part C #56, Domain F). The backlog line is a one-liner, not a checkbox list: "Internal Controls (Maker/Checker) — fully covered in Part A.5, plus a periodic audit report scanning for any possible self-approval cases." Part A.5 (`assertDifferentActors` + a DB `CHECK` per pair) was verified intact — this process's only new work is the report. New module `apps/api/src/modules/internal-controls/` (`internal-controls.{config,service,controller}.ts` + `internal-controls-audit.scheduler.ts`), the `SlaDashboardModule` shape (aggregates other modules' data, owns none of it). **`MAKER_CHECKER_REGISTRY`** is a hand-built inventory of all 15 same-table maker/checker pairs this codebase's migrations have added a DB `CHECK` for (one entry per constraint, traced back to five separate migrations from `20260826091424` through `20260906120000`), plus THREE flagged `dormant: true` (`DisposalBatch`/`DataSharingApproval`/`DataProcessingAgreement` — M06/M07/M08 aren't built yet, no writer exists, the #48 dormant-classifier precedent: scanned anyway, forward-compatible). **A 16th pair is handled separately, not in the registry**: `PolicyChecking.checkedByUserId` vs the PARENT `Policy.issuedByUserId` — this process's own build RESOLVED an open question `policy-lifecycle.md` had flagged (should the DB `CHECK` extend to `issuedByUserId`?) with a firm answer: it structurally CANNOT — a Postgres `CHECK` constraint compares columns on one row of one table, and these two columns live on different tables. This periodic scan is that one pair's ONLY defense-in-depth backstop beyond the app-level `assertDifferentActors` guard, by design, not oversight. **A genuine, non-cosmetic fix found during the build, not a review comment**: the scan's 16 independent read queries were originally sequential (`for...of` with `await` each), which timed out a 30-second e2e test against this session's own long-lived, cumulative `db-test` database; switched to `Promise.all`, cutting the same scan to under 12 seconds — a real production-quality concern (a report that's this slow is bad regardless of test timeouts), not just a test-suite accommodation. New permission `internal-controls.audit` (`[COMPLIANCE_OFFICER, EXECUTIVE_MANAGEMENT, EXTERNAL_AUDITOR]`) — genuinely NEW, not pre-seeded ahead of time the way #55's four were (148 → 149 perms). **A clean run (zero violations) is the EXPECTED outcome, not a surprise** — every same-table pair is already DB-enforced, so this exists as independent verification ("we scanned, and found none," not "trust the code has a guard"), which shaped the whole test strategy: the e2e proof of GENUINE detection had to target the one pair with no DB backstop (planted directly via Prisma, bypassing `assertDifferentActors` entirely, since Postgres itself rejects a planted violation on any of the other 15); every same-table pair's classification logic is proven correct via pure-function unit tests instead (a mocked row is flagged; Postgres would reject that same row for real). `apps/web/` gains an **"Internal controls"** screen (`app/(app)/internal-controls/page.tsx` — stat cards, a violations table shown only when non-empty, a by-pair breakdown table, a manual "Run audit now" button). **A `react-hooks/set-state-in-effect` ESLint false-positive was hit and worked around** — `void load()` directly in a mount effect was flagged even though `load`'s only `setState` calls happen after an `await` (the exact shape several other pages in this codebase already use without issue); the fix already exists as prior art in `apps/web/app/(app)/incidents/page.tsx`: wrap the call as `void (async () => { await load(); })();` instead of `void load();` — noted here because the root cause was never fully explained by the rule's own diagnostics, only empirically bisected. **Verification**: +21 api unit (`internal-controls.config.spec.ts` 15, `internal-controls.service.spec.ts` 6) → api unit **1820** (127 files, from 1799). New `test/internal-controls.e2e-spec.ts` **2/2** (permission gating + full 16-pair report shape; a live-planted cross-table violation detected end-to-end, then restored before finishing — the #51 restore-before-finishing precedent scaled to one row). Full api unit suite 1820/1820 confirmed green; full e2e and Playwright suites pending final confirmation at push time. New Playwright `internal-controls.spec.ts` 4/4. `npm run typecheck`/`lint`/`build` (api + web) OK. | Read `meta/context/internal-controls-audit.md` — especially "The one pair no DB CHECK can express" and "How the scan runs" — before touching #56 or adding any future maker/checker pair to `MAKER_CHECKER_REGISTRY` — "a Postgres CHECK constraint can only compare columns on ONE table — a pair spanning a parent/child relation (like PolicyChecking/Policy) needs a trigger, a denormalized column, or (as done here) a periodic scan; it can never get a plain CHECK", "16 independent read-only Prisma queries against a long-lived dev/test database is slow enough to matter for real — run them concurrently (Promise.all), not sequentially, and this was caught by an e2e timeout, not a review", "a clean self-approval scan is the EXPECTED result, not a surprise, given every pair but one is already DB-enforced — proving genuine detection end-to-end means targeting the one ungated pair specifically, not asserting zero on a shared cumulative database", and "`void asyncFn()` directly in a mount `useEffect` can trip `react-hooks/set-state-in-effect` even when every setState inside happens after an await — wrap it as `void (async () => { await asyncFn(); })();` instead, matching `apps/web/app/(app)/incidents/page.tsx`'s existing pattern" are all easy to get wrong. |
| 2026-09-06 | **NEW `meta/context/incident-management.md`** (Process 55) — filed via `/brain-gap` at `ibms-app` (backlog Part C #55, Domain F). Three checkboxes: the full 7-state chain (Reported→Contained [4h target, critical]→Impact Assessed→Classified [Material needs DPO + Senior Management co-sign]→Notified→Recovered→Closed [root cause mandatory]), senior-management notification within 1 hour of Material classification (job), and independent multi-regulator notification (CBJ, National Cybersecurity Centre, Personal Data Protection Council). **This model is ALSO Part D's M09 (Incident & Breach Management)** — `pcms-privacy-modules.md`'s own summary/citation match `IncidentReport`'s doc comment verbatim; one build now satisfies both. **Genuinely almost no migration** — `IncidentReport` (core schema) pre-existed with every field including both maker/checker columns; `WORKFLOW_TRANSITIONS.IncidentReport` (a strict linear 7-state chain) and `SLA_REGISTRY`'s `incident_containment` (4h)/`incident_senior_management_notification` (1h) also pre-existed — this is their first real consumer. Migration `20260906120000` adds ONLY the missing `IncidentReport_classification_maker_checker_distinct` CHECK. No seed change — 4 permissions pre-seeded (`incident.report` broad-8-roles, `incident.contain`, `incident.classify` shared by DPO+Executive Management, `incident.notify-regulator`). **`incident.classify` is the first permission in this codebase's seed grid shared by BOTH roles of a maker/checker pair** rather than two distinct codes — segregation is enforced one layer deeper via an in-service `actor.roles.includes(...)` role check (`classify()` requires DPO, `coSign()` requires Executive Management) plus `assertDifferentActors` plus the new CHECK; a dedicated unit test proves a single dual-role user is still blocked from self-classify-and-co-sign. **No bespoke scheduler for "senior management notification (job)"** — `classify()` starts the pre-existing 1-hour SLA timer on Material classification, `notifySeniorManagement()` is a manual stamp (confirmed via repo-wide grep: zero real email/SMS/notification infrastructure exists anywhere in this codebase), and the ALREADY-RUNNING generic `SlaTimerScheduler` (every 15 minutes, backlog A.8 — `IncidentReport` was already in `SLA_DASHBOARD_SENSITIVE_ENTITY_TYPES`) is the "(job)" that catches a missed manual notification. The Material co-sign gate is re-derived LIVE (the #16/#51 pattern) in TWO places — `notifyRegulators()` (one of #55's three checkboxes) and `notifyAffectedSubjects()` (M09's own PDPL scope, not one of the three) — both re-check `classification === 'MATERIAL' && !seniorManagementCoSignUserId` immediately before their own write. Reads are audited (`isSensitiveDataAccess: true`) despite no `DataClassification` field on the model — reasoned closer to `Claim`/`TransactionMonitoringAlert`/DSR than the Confidential-tier no-audit precedent. **`@code-reviewer` (mandatory — a new maker/checker pair + new DB CHECK + new workflow entity + breach data) → CHANGES REQUESTED → resolved: 1 MAJOR** (`title` was missing the `NO_FULL_ACCOUNT_NUMBER` guard `description` already had — a reporter's first, least-considered input during an active incident) **+ several MINORs** (`maker-checker-segregation.md` gained an `IncidentReport` row plus a new "shared permission across two roles" exception clause; this very context file not existing at review time — the #51 MAJOR-3 lesson again; `notifyAffectedSubjects()` was missing the co-sign gate `notifyRegulators()` had — fixed for parity; the web page showed the Classify buttons to an Executive-Management-only user and the Co-sign button to a DPO-only user, both an always-403 dead end — fixed by splitting visibility to the specific role each sub-action needs) **+ a NIT** (the explicit dual-role unit test above). Also caught and fixed twice, unrelated to the review: a literal `*/15 * * * *` cron string inside a `/** */` JSDoc comment prematurely closes the comment block at the `*/` — reworded to "runs every 15 minutes" in both `incident.service.ts` and `compliance-risk.module.ts`. `apps/web/` gains an **"Incident Management"** screen. **Verification**: +47 api unit (`incident.config.spec.ts` 10, `incident.service.spec.ts` 37) → api unit **1799** (125 files, from 1752); new `test/incident.e2e-spec.ts` 3/3 (full CRITICAL→MATERIAL→co-sign→notify lifecycle, a NON_MATERIAL skip-the-gate path, and permission/filter coverage); Playwright `incidents.spec.ts` 4/4. Full api unit suite 1799/1799 confirmed green post-review-fix; full e2e and Playwright suites pending final confirmation at push time. | Read `meta/context/incident-management.md` — especially its "`@code-reviewer` findings (resolved)" section — before touching #55/M09 or any future maker/checker pair sharing one permission across two roles — "a shared-permission maker/checker pair needs an in-service role check on TOP of `assertDifferentActors` + a DB CHECK, and a dual-role user must be tested explicitly, not just inferred", "a control that fires only on a scheduler catching a missed manual step still needs the manual step to exist and be logged — check whether real notification infrastructure exists before assuming a 'job' checkbox needs a new one", "a live-recomputed gate protecting one irreversible external action (regulator notification) protecting a SECOND, equally irreversible one (affected-subject notification) needs the SAME gate in both places, not just the one the backlog named explicitly", and "NEVER embed a literal cron string containing `*/` inside a `/** */` JSDoc block comment — it closes the comment early and corrupts everything after it until EOF" are all easy to get wrong. |
| 2026-09-05 | **NEW `meta/context/operational-pi-risk.md`** (Process 53-54) — filed via `/brain-gap` at `ibms-app` (backlog Part C #53-54, Domain F). Two checkboxes: "a generic risk register covering the six categories the source names — Professional Indemnity gets its own, deeper table" and "track the broker's own Professional Indemnity policy (coverage limit, expiry, claims history) — a broker without valid PI cover is itself a licensing breach." **Genuinely no migration, no seed change** — `RiskRegisterItem`/`ProfessionalIndemnityPolicy`/`ProfessionalIndemnityRiskEvent` (Part 5/7.1 core schema) all pre-existed; `risk-register.manage` (`[COMPLIANCE_OFFICER, BRANCH_DEPARTMENT_MANAGER]`) / `pi-policy.manage` (`[COMPLIANCE_OFFICER]`) pre-seeded. `ProfessionalIndemnityRiskEvent` already had a real writer before this landing — `PolicyCheckingRepository.recordChecking` (Process 20) auto-logs one on a coverage discrepancy; this gives those rows their first read surface, not their first writer. **`ProfessionalIndemnityPolicy` is deliberately NOT a fixed-id singleton like `BrokerLicense`** (Process 51) — no `issuedAt`/period-start field to define a validity window with, so a renewal is a brand-new row (preserving `claimsHistorySummary` per period) and "current" = `findFirst({orderBy:{expiresAt:'desc'}})`, the SAME definition `PolicyCheckingRepository.findLatestPiPolicyId` already needed before `PiPolicyRepository` existed to give it a home — that method now delegates to the new repo instead of carrying its own copy. **`@code-reviewer` (mandatory — a shared repository refactor touching a maker/checker-adjacent module + a new derived-"current" resolution over money/PII-adjacent data) → CHANGES REQUESTED → resolved: 1 BLOCKER + 1 MAJOR.** **BLOCKER**: `findFirst({orderBy:{expiresAt:'desc'}})` alone has no secondary sort key, and a tie is the LIKELY case not an edge case — `parseCalendarDate` normalizes any plain-date input to an exact UTC midnight, so two officers entering the same renewal date (or a multi-insurer PI tower, or an accidental duplicate — no uniqueness guard exists) tie to the millisecond, and Postgres gives no guarantee which tied row wins, so `GET /pi-policy/current`, `list()`'s `isCurrent` flag, and the discrepancy auto-link could each independently pick a different "current" record; fixed by adding `id: 'desc'` as a deterministic secondary key on both `findCurrent()` and `findMany()`. **MAJOR**: the new `piRiskEventAuditSnapshot` embedded an auto-logged event's `description` verbatim into the audit row — but for an auto-logged event that text is `piRiskEventDescription()`'s coverage-figure diff, the SAME content `policyCheckingAuditSnapshot` deliberately excludes from the sibling `PolicyChecking` audit row (`sensitive-data-handling.md` — "metadata not body"); fixed by redacting `description` to a placeholder whenever `sourcePolicyCheckingId` is set, leaving a MANUAL entry's own narrative verbatim. **2 MINORs** (a manual risk event auto-resolving to a lapsed current PI policy now gets a durable signal — a `logger.warn` + `linkedPolicyWasLapsedAtLogTime` in the CREATE audit row, rather than a new N+1-prone view field; the claims-history update's lack of an optimistic-concurrency guard is now an explicit stated trust-level decision, not implicit) **+ 1 deferred MINOR** (none of the three models carry a `DataClassification` field — documented as a real open gap needing an actual PCMS determination, not fixed by extending the `BrokerLicense.scopeOfAuthorization` analogy further, since `claimsHistorySummary` and a risk event's coverage detail plausibly name identifiable customers and settlement amounts). A genuine cross-run test-monotonicity bug was also caught and fixed while verifying the tiebreak: the new e2e spec's own `nextFarFuture()` helper originally grew its offset by a whole extra DAY per call within a run, which broke monotonicity ACROSS runs (a later-position call in an earlier run could out-rank an earlier-position call in a later run, since the wall-clock gap between runs is always under a day) — fixed by keeping the offset fixed and using only `Date.now()` plus a millisecond counter. `apps/web/` gains an **"Operational & PI risk"** screen (one page, three sections). **Verification**: +47 api unit (`risk-register.config.spec.ts` 5, `risk-register.service.spec.ts` 11, `pi-policy.config.spec.ts` 7, `pi-policy.service.spec.ts` 8, `pi-risk-event.config.spec.ts` 4, `pi-risk-event.service.spec.ts` 12) → api unit **1752** (123 files, from 1705); new `test/operational-pi-risk.e2e-spec.ts` 4/4 (incl. a deliberate tie-on-`expiresAt` regression test proving 5 repeated `/current` reads all agree); `policy.e2e-spec.ts`'s pre-existing discrepancy-to-PI-risk-event auto-link re-verified unaffected by the repository refactor. Full api e2e suite green aside from the 2 documented chronic flakes (rbac confirmed passing in isolation; up-sell confirmed as a Prisma-engine-connection/timeout error unrelated to this diff, not an assertion failure); Playwright `operational-pi-risk.spec.ts` 3/3, full suite 171/171 (from 168). `npm run typecheck`/`lint`/`build` (api + web) OK. | Read `meta/context/operational-pi-risk.md` — especially its "`@code-reviewer` findings (resolved)" section — before touching #53-54 or building any other derived-"current"-over-a-table resource — "`orderBy` on a field a caller can supply (even a plain calendar date) ALWAYS needs a deterministic secondary sort key — a tie is the likely case, not a theoretical one", "an audit snapshot for a row with two different writers (an internal auto-link vs. a manual entry) needs per-writer content rules, not one blanket 'include verbatim' — check what the OTHER writer's sibling audit row already excludes before assuming this one is fine", "`ProfessionalIndemnityPolicy` is NOT a singleton like `BrokerLicense` — a renewal is a new row, `findCurrent()` picks the furthest-out expiry", and "a test helper generating 'unique future dates' across repeated runs against a cumulative db-test needs its monotonicity to survive BOTH same-run ties AND cross-run reordering — growing an offset by a whole day per counter step breaks the second one" are all easy to get wrong. |
| 2026-09-05 | **NEW `meta/context/data-subject-requests.md`** (M04) — filed via `/brain-gap` at `ibms-app` (backlog Part D, bundled under Process #52 Data Protection Compliance). Second of Part D's nine systems, after M03 (Consent Management) — both now share `apps/api/src/modules/pdpl/`. **`DataSubjectRequest` (Part 4.1 core schema) pre-existed** with every field the ACCESS/CORRECTION/DELETION/OBJECTION workflow needs except the maker/checker closure pair; `WORKFLOW_TRANSITIONS.DataSubjectRequest` and `SLA_REGISTRY`'s two DSR entries (`dsr_access_deletion` 15 business days, `dsr_correction_objection` 10, both with the pre-existing DPO-then-General-Manager two-stage escalation) also pre-existed — this module is their first real consumer. Migration `20260905120000` adds `processedByUserId`/`closedByUserId`/`rejectionReason`/`noOpenRetentionHoldConfirmedAt`, the `DataSubjectRequest_closure_maker_checker_distinct` CHECK, and three list-filter indexes — no seed change (`dsr.log`/`dsr.handle`/`dsr.close`, all pre-seeded). **Closure needs sign-off from a DIFFERENT DPO officer than whoever processed the request** (`assertDifferentActors` + the CHECK) — `maker-checker-segregation.md`'s own "Covered actions" table was missing this row despite this file's own "Mandatory rules" summary already naming "DSR closure" as covered; fixed alongside this build. A DELETION request needs an explicit, now-PERSISTED staff attestation (`confirmNoOpenRetentionHold` → `noOpenRetentionHoldConfirmedAt`) before it can be marked fully FULFILLED — Retention & Disposal (M06) isn't built yet, so this is deliberately a staff attestation, not an automated check (the #48 `third_party_payment_source`-dormancy lesson applied). The one ACCESS-only +15-business-day extension re-bases `slaDueAt` and its `SlaTimer` pair via a new `SlaTimerService.resolve()` parameter (`createdBefore`), START-then-resolve rather than the naive resolve-then-start. **`@code-reviewer` (mandatory — a new `WorkflowTransitionService` entity + a new maker/checker pair + Highly-Confidential-adjacent PDPL rights data) → CHANGES REQUESTED → resolved: 1 BLOCKER** (the extension's write-once repository guard needed a `status` re-assertion alongside `accessExtensionAppliedAt IS NULL` — without it, a DSR that concluded between the service's read and the repository's write could still have its deadline silently re-based and a fresh `SlaTimer` pair opened against an already-closed-out request) **+ 2 MAJORs** (the SLA re-basing's naive resolve-then-start ordering could leave a DSR with ZERO open timers if the follow-up `startTimer` failed after a successful `resolve` — fixed by reordering to start-first with a `createdBefore` cutoff, so every partial-failure outcome is the safer of the two possible bad states, never a silent gap; the DELETION attestation was validated in memory and discarded, leaving no persisted or audited trace of which DPO officer attested it — fixed by persisting `noOpenRetentionHoldConfirmedAt`) **+ 2 MINORs** (the race-safe idempotent-retry-on-ConflictException pattern, present only on `start()` at review time, applied uniformly across all six status-changing methods; the "logged the same business day" comment on `receivedAt`'s non-backdatable design overclaimed what it actually guarantees — softened, not changed). DSR reads are audited (`isSensitiveDataAccess: true`) — a deliberate departure from the Confidential-tier no-read-audit precedent (#33/#34/#41/#44/#45/#46/#51), reasoned as closer in kind to `Claim`/`TransactionMonitoringAlert`. `apps/web/` gains a **"Data Subject Requests"** screen. **Verification**: +~40 api unit (incl. the review-fix regression tests) → api unit **1705** (117 files, from 1695 pre-review-fix); new `test/dsr.e2e-spec.ts` 3/3 (incl. a real-Postgres assertion that extension leaves exactly 2 open + 2 resolved `SlaTimer` rows, never 0 or 4); full api e2e suite green, all 33 files / 147 tests, no regressions this run; Playwright `dsr.spec.ts` 3/3, full suite 168/168 (from 165). `npm run typecheck`/`lint`/`build` (api + web) OK. | Read `meta/context/data-subject-requests.md` — especially its "`@code-reviewer` findings (resolved)" section — before touching M04 or any future workflow entity with an SLA-timer extension/re-basing need — "a status-conditional `updateMany`'s `where` must re-assert EVERY field the caller validated between the read and the write, not just the one field being written (`race-safe-invariants.md`, generalized again)", "re-basing an SLA timer pair with `SlaTimerService`'s existing resolve+startTimer primitives must go START-then-resolve with a `createdBefore` cutoff when old/new share a `workflowName` — the naive resolve-then-start order can leave zero open timers on a partial failure", "a staff-attestation control that gates a terminal transition must be PERSISTED and carried into the audit snapshot, not just checked in memory and discarded", and "the maker-checker-segregation.md lex table can lag CLAUDE.md's own summary — cross-check both, and fix the lex file the day a gap surfaces" are all easy to get wrong. |
| 2026-09-05 | **NEW `meta/context/regulatory-compliance.md`** (Process 51) — filed via `/brain-gap` at `ibms-app` (backlog Part C #51, Domain F). Two checkboxes: "automatically block new business issuance once the license lapses" and "a compliance calendar of regulatory obligations with owner, due date, and evidence-of-submission tracking." Also verified same-day: **#50 Conflict of Interest needed no separate build** — covered under #16's `ConflictOfInterestDisclosure`/`RecommendationService`, confirmed still intact against current code. **Genuinely no migration, no seed change** — `BrokerLicense`/`ComplianceCalendarItem` (Part 7.1 core schema) pre-existed; `license.manage`/`compliance-calendar.manage` (both `[COMPLIANCE_OFFICER]`) pre-seeded. **`BrokerLicense` is a true SINGLETON** — one row at a fixed id (`BROKER_LICENSE_SINGLETON_ID`), not a `findFirst()` guess. **The lapse check is a pure LIVE recompute (`isBrokerLicenseCurrentlyLapsed`), not a scheduler-maintained flag** — the #16 MAJOR lesson generalized: a block-issuance control must be correct the instant the underlying condition changes, not only after a sweep next runs — so there is deliberately **no `BrokerLicenseSweepScheduler`** in this codebase, the only "goes stale" backlog item without one. `PolicyService.assertLicenseNotLapsed` is the literal first statement in `place()`; an unconfigured license is deliberately NOT blocked (load-bearing for dozens of existing tests). `ComplianceCalendarItem.recordSubmission` is write-once (409 on repeat). `parseCalendarDate` promoted `policy.config.ts` → `common/calendar-date.util.ts` (3rd consumer). **`@code-reviewer` (mandatory — a business-critical issuance-blocking gate + a novel global-singleton e2e-testing pattern) → CHANGES REQUESTED → resolved: 3 MAJORs.** **MAJOR 1**: a `findCurrent() === null` pre-check alone is not race-safe for a create-once singleton — two concurrent `POST /broker-license` calls both pass it before either writes; the fixed-id PK stops a second row from ever existing, but the loser's unhandled P2002 surfaced as a raw 500 instead of a clean 409; fixed with the same `isUniqueViolation` catch-and-rethrow shape every other create-once resource in this codebase uses. **MAJOR 2**: the new e2e specs are the first in this codebase to mutate a real, shared, non-test-scoped singleton (`BrokerLicense`) and restore it before finishing — the restore's own success check originally only verified the outcome when the renew call itself returned 201, missing the more likely failure (renew returning non-201), which would silently leave the singleton lapsed and cascade into unrelated 422s across later e2e files; fixed by asserting the renew status unconditionally (and separately restructured away from `throw`-in-`finally`, which ESLint's `no-unsafe-finally` correctly flags as swallowing a genuine test failure). **MAJOR 3**: this very documentation pass was itself the missing piece at review time. **1 MINOR**: the web renew form only collected 2 of 4 fields, silently wiping the other two on a full-replace `renew()` — fixed by pre-filling all fields from the current record, adjusted during render (not a `useEffect`, to avoid clobbering an in-progress edit). **Verification**: +33 api unit → api unit **1651** (114 files, from 1617); new e2e `regulatory-compliance.e2e-spec.ts` 2/2 + `policy.e2e-spec.ts` +1 (the real cross-module block, singleton-safe); full api e2e green aside from the 2 known chronic flakes + 1 confirmed-transient contention failure; Playwright 165/165 (incl. 3 new). | Read `meta/context/regulatory-compliance.md` — especially its "`@code-reviewer` findings (resolved)" section — before touching #51 or building any other singleton/create-once resource — "a pre-check-then-write is never sufficient on its own for 'at most one' — the write itself must also handle losing the race, even when the DB PK was never actually at risk", "an e2e test mutating real global (non-test-scoped) state must verify its own restore succeeded, not just attempt it — and never via `throw`-in-`finally`", "the lapse check is a live recompute wherever needed, not a scheduler-maintained flag — there is deliberately no scheduler here, unlike every sibling 'goes stale' backlog item", "an unconfigured BrokerLicense is NOT treated as lapsed — load-bearing for the existing test suite, not an oversight", and "#50 needed no separate build — verify against #16's real code before assuming a gap" are all easy to get wrong. |

---

## What this brain is for

**IBMS — Insurance Brokerage Management System.** A system for a licensed insurance/reinsurance broker operating in Jordan, covering the full loop from lead through claims and renewal, built to CBJ insurance regulation, Jordan PDPL No. 24/2023, and ISO/IEC 27001 + 27701.

This brain is seeded **before** a line of application code exists — from the approved Business & Technical Context Document and the already-approved Privacy Compliance Management System (PCMS) toolkit (1 policy, 4 standards, 10 procedures, 10 forms, 1 SRS), not from PR review history. That is a deliberate substitution of Input 1 in `INTAKE.md`: the "rules that would block a PR" already exist as signed-off regulatory documents. Once an engineering repo exists, add PR-derived lex the normal way and this note can go.

**Broker legal name:** not yet supplied to this brain — replace throughout once known. It does not block anything below.

---

## Staying current

Sync at session start — fetch, and pull only if behind **and** clean **and** on `main`. If dirty or on a feature branch, notify instead of pulling. Rule: `meta/lex/brain-freshness.md`.

---

## Repo map

```
meta/lex/         Mandatory rules. Read before non-trivial work.
meta/context/     How things actually work here. Read before touching an area.
meta/designs/     Why things are the way they are. Read before changing a decision.
meta/agents/      Agent definitions (source of truth).
meta/guides/      Advisory. Setup, onboarding, contributing.
meta/templates/   PR, ticket, and doc templates.
.claude/agents/   Mirror of meta/agents/ — what Claude Code actually loads.
.claude/hooks/    Enforcement scripts.
.claude/commands/ Slash commands.
```

---

## Modules (business view — not yet mapped to repos)

`ibms-app` exists as a single web+api monorepo — it is not yet split by module or service boundary. What exists is the module inventory from `PRIV-SRS-01` and the 74-process/8-domain inventory in the context document. Treat this as the module list, not a services table, until a service-boundary decision fills it in:

| Module | Governs |
|---|---|
| Core IBMS (Sales/CRM, Policy, Claims, Finance) | The 74 business processes — see `meta/context/policy-lifecycle.md` and `meta/context/claims-lifecycle.md` |
| M01–M12 (PCMS) | Privacy/compliance modules — see `meta/context/pcms-privacy-modules.md` |

## Architecture relationships

**IBMS and the PCMS (Privacy Compliance Management System) are one system, not two.** PCMS is the source of truth for every privacy/consent/retention/breach/DSR rule; IBMS's compliance module consumes PCMS decisions and feeds it data (customers, policies, claims) — it must never re-derive or duplicate a privacy rule. See `meta/designs/2026-08-pcms-source-of-truth.md`.

`ibms-app` is a Next.js (web) + NestJS (api) + PostgreSQL/Prisma monorepo. See `meta/designs/2026-08-ibms-app-stack-and-repo-split.md` for why, including the Prisma 6-vs-7 and repo-split calls. No call-direction / auth-boundary / system-of-record decision beyond "web calls api" has been made yet. **Do not invent one.** Record it here the day it's decided.

`ibms-app` vendors this repo as a pinned git submodule (`ibms-app/ibms-brain/`) rather than restating any rule — see `meta/designs/2026-08-ibms-app-brain-submodule-sync.md`. This repo has no reverse dependency on `ibms-app` and does not need one.

---

## Common commands

**This repo (`ibms-brain`) has none — it stays documentation-only.** For `ibms-app` (the engineering repo): `npm install`, `npm run dev`, `npm run test`, `npm run e2e` — see its own `README.md`/`CLAUDE.md`, not this file, for the full list and any changes to it.

## Environment

**This repo needs none.** `ibms-app` pins Node `20.13.0` (`.nvmrc`) and requires Docker — see its `README.md`. Record changes there, not here.

---

## Agents

| Agent | Use for |
|---|---|
| `@code-reviewer` | Review before push. **Mandatory** for any workflow/approval logic, financial (premium/commission/claim) calculation, or code touching Confidential/Highly Confidential data. |
| `@software-developer` | Implementation, bug fixes, refactoring. |

Definitions are source-of-truth in `meta/agents/` and mirrored to `.claude/agents/` by `.claude/hooks/mirror-agents.sh`. **Never hand-copy.**

---

## Mandatory rules

Loaded from `meta/lex/`. Enforcement level is stated in each file.

| Lex | Governs |
|---|---|
| `money-decimal-jod.md` | Decimal only for premium/commission/claim amounts |
| `workflow-state-transitions.md` | Never assign a workflow `status` directly — always through a transition function |
| `race-safe-invariants.md` | A "one of these / only once" invariant is a DB constraint or a status-conditional write, never a `findMany().find()` check-then-act |
| `maker-checker-segregation.md` | No self-approval on KYC, policy checking, refunds, disposal, DSR closure |
| `sensitive-data-handling.md` | Highly Confidential data (medical, financial, national ID, UBO) never logged/exported/shared unencrypted |
| `pdpl-sla-timers.md` | Every statutory SLA (consent withdrawal, DSR, breach containment, disposal) is a tracked deadline, not documentation |
| `backup-rpo-rto.md` | Encrypted backups + an actually-tested restore drill, not backup-only assurance |
| `kyc-aml-sla-timers.md` | KYC compliance-review turnaround + periodic re-KYC cadence are tracked deadlines (CBJ AML domain, not PDPL) — values currently draft/unsourced |
| `code-review.md` | Review format and severity levels |
| `definition-of-done.md` | No push without evidence from the verification contract |
| `workspace-updates.md` | Keeping this file and README current |
| `brain-freshness.md` | Session-start sync |

---

## What "working" means

`meta/context/verification-contract.md` defines the gates every change must pass and the evidence each produces. Run them with `bash scripts/verify.sh`. Claims are not evidence — exit codes and screenshots are. **In this repo, the only real gate is `bash scripts/brain-doctor.sh`** — it stays documentation-only. For `ibms-app` changes, real gates now exist: `npm run typecheck|lint|test|build|test:e2e|e2e` — see `meta/context/verification-contract.md` § Backend/frontend gate commands. Do not deploy to production; open a PR with evidence attached and let a human merge.

Path-scoped rules live in `.claude/rules/` and load themselves when you touch matching files — none exist yet because there is no code to scope them to.

## Domain glossary

See `meta/context/glossary.md`. Terms that mean something specific here — **account**, **customer**, **policy**, **loss ratio**, **maker/checker** — go there, not in this file.

---

## MR standards

**Not yet decided** — there is no engineering repo. Record branch naming, PR title format, and review rule here the day one is created. Until then, `meta/templates/pr-description.md` is the description format for any interim proposal/ticket writing.

---

## meta/ structure rules

1. Agent definitions are source-of-truth in `meta/agents/`. The mirror to `.claude/agents/` is automated — do not hand-copy.
2. Designs go in `meta/designs/<area>/` with a descriptive filename. No free-form naming.
3. A file belongs in `lex/` only if it has a filled "How it is enforced" section. Otherwise it goes in `guides/`.
4. Do not create a folder before it has content.
5. All cross-document links point to **this repo**. The PCMS documents (`PRIV-STD-*`, `PRIV-SOP-*`, `PRIV-FRM-*`, `PRIV-SRS-*`) are the canonical location for privacy/compliance detail — this brain cross-references them, it never restates them as a second source of truth.

---

## Mindset

Research before architecture. Architecture before execution.

`context/` exists so nothing has to be explained twice — to a human or to an agent.

The brain grows by use. When an agent asks something this repo should have answered, that gap is the next file. Run `/brain-gap`.
