@AGENTS.md

# CLAUDE.md — ibms-brain

Workspace instructions. Loaded into every Claude Code session in this repo and in every engineering repo that syncs the brain, once those repos exist.

<!-- The @AGENTS.md line above is an import, not a link. Claude Code reads CLAUDE.md and
     NOT AGENTS.md, so without that import the AGENTS.md file is dead weight for Claude
     while still being read by other agent tools. Keep the import on line 1.
     Target for this file: under 200 lines. Longer files reduce adherence. -->

---

## What's New

| Date | Change | Action required |
|------|--------|-----------------|
| 2026-09-04 | **NEW `meta/context/transaction-monitoring.md`** (Process 48) — filed via `/brain-gap` at `ibms-app` (backlog Part C #48, Domain F — opens Compliance & Risk beyond KYC). Also verified same-day, immediately before #48: **backlog #47 KYC needed no separate build** — its own line reads "fully covered under #3–4", confirmed against the real, built `KycService`/`ScreeningService` code. **`TransactionMonitoringAlert` (Part 7.2 core schema) pre-existed with no application code ever writing to it** — this is its first consumer. **Migration `20260904130000` only WIDENS**: `sourceEntityType`/`sourceEntityId` (nullable — the triggering `Receipt` for the two event-scoped patterns; null for the two aggregate patterns), three indexes, and two race-safe uniqueness guards. **No seed change** — `aml.monitor`/`aml.escalate` (both `[COMPLIANCE_OFFICER]`, module `compliance-risk`) were already seeded (149 perms), kept as two distinct permissions on purpose even though both grant to the same role today. **Not a `WorkflowTransitionService` entity, no maker/checker** (`aml.monitor` vs `aml.escalate` is the #42 `complaint.escalate` shape, not a dual-approver gate). **Detection reads existing Finance/Endorsement data, no new business process**: `large_premium_payment`/`third_party_payment_source` scan every `Receipt` (#32 — an actual collected payment, not a raised-but-unpaid `Invoice`) against a **DRAFTED/UNSOURCED** `AML_LARGE_PREMIUM_THRESHOLD_JOD = '15000.000'` and whether the `Receipt`'s `PaymentChannel` (#38) belongs to a different customer than the one invoiced; `frequent_cancellations`/`frequent_refunds` count `Cancellation`/`Refund` per customer (via `Endorsement.policy.customerId`) in a **DRAFTED** rolling 90-calendar-day window against a threshold of 3. **`third_party_payment_source` is DORMANT in production, not merely gapped** (a `@code-reviewer` BLOCKER, caught and documented, not fixed by relaxing the underlying rule) — `CollectionService.assertReceiptChannelUsable` (#38) already rejects any real `Receipt` whose channel mismatches the invoiced customer before one can exist, so this classifier can never fire outside a test that bypasses `CollectionService` directly; kept as a forward-compatible detector. **Two DIFFERENT race-safe shapes**: a plain `@@unique([patternType, sourceEntityId])` for the event-scoped patterns (Postgres treats every NULL `sourceEntityId` as distinct, so this is inert for the two aggregate patterns AND for a manual log) plus a hand-authored partial `UNIQUE ("customerId","patternType") WHERE status='open' AND "patternType" IN ('frequent_cancellations','frequent_refunds')` (the `UpSellRecommendation`/`ClaimFollowUpAlert` shape) for the aggregate ones — **scoped directly to `patternType`, NOT to `sourceEntityId IS NULL`** (a `@code-reviewer` BLOCKER on the first pass: a `sourceEntityId IS NULL` predicate would also have collided two unrelated manual `other`-pattern alerts for the same customer, throwing an uncaught 500 — `create()` now also catches `P2002` → a 409). **Duplicate alerts are explicitly NOT treated as a hard business-integrity risk here** (unlike a double refund), so the guard is pre-check + `P2002`-catch, not a strict invariant. **The suspicious-activity escalation path is TWO SEPARATE STEPS** (`escalate` then `report-to-authority`, the latter requiring the former first — 422 otherwise), **deliberately mirroring M03's consent-withdrawal request/confirm shape** for the identical reason: an internal decision and an actual external filing are different facts. **Record-keeping is the row's own immutability** (no delete endpoint anywhere on this model — the row + its audit trail IS the record) — **the actual retention PERIOD is undocumented/unsourced and NOT built as a tracked `SlaTimer` deadline** (unlike `kyc-aml-sla-timers.md`'s two figures) because no disposal mechanism exists anywhere yet to guard against, the same honest gap `data-retention-and-disposal.md` documents for PDPL. `detailText` carries `NO_FULL_ACCOUNT_NUMBER` and is **excluded from every audit row** (the #44/#45 precedent) — the model's own default `classification` is `HIGHLY_CONFIDENTIAL`, so unlike the Confidential-tier #33/#34/#41/#44/#45 reads, `get()`/`list()` DO write a best-effort `READ` audit row (the `Claim`/`Crm` same-tier precedent — a `@code-reviewer` MAJOR on the first pass, which had wrongly followed the Confidential-tier no-audit precedent instead). **`@code-reviewer` → CHANGES REQUESTED → resolved: 2 BLOCKERs** (the partial-index scoping above; the dormant classifier above, documented not architecturally changed) **+ 1 MAJOR** (the READ audit gap above) **+ 2 MINORs** (`escalate()` now checks the escalation flag before the `status` guard, so a closed-but-escalated alert stays idempotent on retry; the sweep's `scanned` count now sums actual `Cancellation`/`Refund` rows examined, not distinct customers — the first pass mixed the two granularities). New `apps/api/src/modules/compliance-risk/` (`transaction-monitoring.{config,service,controller}.ts` + `transaction-monitoring-sweep.scheduler.ts`, nightly 09:00 UTC) + `repositories/transaction-monitoring-alert.repository.ts`. `apps/web/` gains an **"AML monitoring"** screen. **Deferred**: all four thresholds drafted/unsourced; `third_party_payment_source` only catches a `Receipt` with a recorded `PaymentChannel`; no cross-pattern dedup; no case-management beyond `open`/`closed`. | Read `meta/context/transaction-monitoring.md` before touching #48 or the rest of Domain F — "detection reads EXISTING Finance/Endorsement data (Receipt/Cancellation/Refund), it is not a new business process", "`third_party_payment_source` is DORMANT — #38's `assertReceiptChannelUsable` already prevents the scenario it checks for, so it can never fire outside a test that bypasses `CollectionService` — this is documented prominently, do not assume it is a live control", "the aggregate partial-unique index is scoped to `patternType IN (...)`, NOT `sourceEntityId IS NULL` — the latter was a real BLOCKER (it would collide two unrelated manual `other`-pattern alerts)", "escalation is TWO calls (`escalate` then `report-to-authority`), mirroring M03's request/confirm shape — `report-to-authority` 422s if `escalate` hasn't run, and `escalate` itself is idempotent even on an already-closed alert", "record-keeping = the row's own immutability (no delete path) — the retention PERIOD itself is undocumented, NOT a tracked SlaTimer deadline (no purge mechanism exists to guard against yet)", "reads ARE audited here (HIGHLY_CONFIDENTIAL, the `Claim` precedent) — do not assume the Confidential-tier no-audit precedent applies", and "#47 KYC needed no separate build — verify against #3-4's real code before assuming a gap" are all easy to get wrong. |
| 2026-09-04 | **NEW `meta/context/consent-management.md`** (M03) — filed via `/brain-gap` at `ibms-app` (backlog Part D §5.1 / `IMPROVEMENTS.md` §5.1, bundled under backlog Process **#52 Data Protection Compliance**). Opens **Part D / PCMS** — the first of the nine Part D systems, and the first of PCMS's M01-M12 to get real code. **`ConsentRecord` (Part 4.1 core schema) pre-existed and needed NO widening — genuinely no migration.** `consent.manage` (`[SALES_RELATIONSHIP_OFFICER, PLACEMENT_TECHNICAL_OFFICER, CLAIMS_OFFICER, DATA_PROTECTION_OFFICER]`) was already seeded — **no seed change** (149 perms) — covering capture, withdrawal, and reads alike (the #41/#44/#45 "one perm for CRUD" shape). **Not a `WorkflowTransitionService` entity** — no `status` field at all, just two independently-stampable timestamps (`grantedAt`, `withdrawnAt`) on an otherwise immutable row; the row's own history across multiple rows for a subject+purpose IS the record (Process 44's pre-existing `evaluateMarketingConsent` already reads it that way). New `apps/api/src/modules/pdpl/` (`consent.{config,service,controller}.ts` + 2 DTOs) + `repositories/consent-record.repository.ts`, the first module registered after `SlaDashboardModule`. **Endpoints** (all `consent.manage`): `POST /consent-records` (`{ customerId? | insuredPersonId?, purpose, granted, consentTextVersion }` — captures a grant OR an explicit decline, recorded either way, never silently dropped); `POST /consent-records/:id/request-withdrawal` + `POST /consent-records/:id/confirm-withdrawal` — a **two-step withdrawal flow**, not one call; `GET /consent-records?customerId=&insuredPersonId=&purpose=&granted=` + `/:id`. **`isMarketing` is DERIVED (`purpose === 'MARKETING'`), never accepted as input** — the "computed, not an input, when derivable" rule (#28/#31/#38/#44) — the mechanism `PRIV-SOP-04`'s "consent and contractual-necessity processing are always two separate, independently-actionable controls" is enforced structurally. **Exactly one of `customerId`/`insuredPersonId` is a service-level 422, NOT a DB CHECK** (unlike `PaymentChannel`'s `owner_exactly_one`, #38) — deliberately lighter-weight, reasoned in the code: a `ConsentRecord` is written by exactly one call site, once, never edited afterward, so there is no concurrent-write race to guard against the way #38's had. **The two-step withdrawal flow gives the pre-existing, previously-unused `consent_withdrawal` `SLA_REGISTRY` entry (2 business days, `PRIV-STD-01` §6.3) a real, meaningful window**: `requestWithdrawal` starts the SLA clock and touches no `ConsentRecord` field (best-effort `SlaTimerService.startTimer`); `confirmWithdrawal` stamps `withdrawnAt` and resolves the timer (best-effort `SlaTimerService.resolve`) — the model's own field comment ("must reflect in register within 2 business days") only makes sense if intake and reflection are genuinely separate events; a single atomic call would make the SLA vacuous by construction. `confirmWithdrawal` also works standalone with no prior request (an immediate self-service withdrawal — `resolve` is a documented no-op when nothing is open). **Feeds Process 44's marketing-send gate for free**: `evaluateMarketingConsent` already reads the live `withdrawnAt` on every send — the moment `confirmWithdrawal` sets it, the backlog's "affected communications suppressed immediately" clause is satisfied by code that shipped before this module did; M03 touches no `CommunicationLog` code at all. Audit: `CREATE` (ids + purpose + the decision, no free text — the model has none), `UPDATE` on the withdrawal (the event only) + the `SlaTimer` engine's own rows. `apps/web/` gains a **"Consent"** screen. **Deferred**: the other eight Part D systems (DSR, Retention & Disposal execution, Vendor Risk, Data Sharing, Incident & Breach, DPIA, notices, RoPA) and the DPO Workspace dashboard — none built yet; no per-subject "current status" read (a caller must apply `evaluateMarketingConsent`-style logic itself for any purpose besides MARKETING, which is the only one with a shared helper today); no bulk/import capture path; `request-withdrawal` can create more than one open `SlaTimer` for the same record under a genuine double-call race (cosmetic only — `resolve` closes all matching rows together, documented, not hardened). | Read `meta/context/consent-management.md` before touching M03 or any of the other Part D systems — "the model pre-existed, genuinely no migration, no seed change", "NOT a workflow entity — just `grantedAt`/`withdrawnAt` on an immutable row, multi-row history IS the audit trail", "withdrawal is TWO calls (`request-withdrawal` then `confirm-withdrawal`), not one — that is what makes the 2-business-day SLA a real window instead of a vacuous one", "`isMarketing` is derived from `purpose`, never an input", "exactly-one-owner is app-level 422, not a DB CHECK (reasoned: single call site, no concurrent-write race)", and "Process 44's marketing gate already reads `withdrawnAt` live — M03 doesn't touch that code, it just finally writes the field" are all easy to get wrong. |
| 2026-09-04 | `meta/context/customer-service-lifecycle.md` gains a **Customer Retention (Process 46)** section (filed via `/brain-gap` at `ibms-app` Part C #46 — the backlog's one checkbox: "Automatically open a retention case on renewal inactivity or lapse risk") — **Domain E is now complete (#41–46)**. **`RetentionCase` (Part 4 core schema) needed no widening; `RenewalCase` (Part 3.9 core schema) already carried `retentionEscalatedAt DateTime?`** — a nullable timestamp clearly provisioned for exactly this mechanism, unused until now. **Genuinely no migration, no seed change** (`retention-case.manage` `[SALES_RELATIONSHIP_OFFICER, BRANCH_DEPARTMENT_MANAGER]` seeded in `a440c1b`; 149 perms). **Built ahead of its data source** (the #8 / #10 / #29 shape) — the renewal module (Part 3.9) that would create a `RenewalCase` per policy nearing expiry is **not built**, so in normal running the sweep is a logged no-op, exactly #29 Loss Ratio's precedent. **Not a `WorkflowTransitionService` entity, no maker/checker, no `SlaTimer`** — a factual log (`status`: plain string `open → closed`, no richer lifecycle, no outcome field — the bare schema has none). New `apps/api/src/modules/customer-service/retention-case.{config,service,controller}.ts` + `retention-sweep.scheduler.ts` + 2 DTOs + `repositories/retention-case.repository.ts`, wired as the **5th `CustomerServiceModule` controller**. **The classifier** (`classifyRenewalCaseForRetention`, pure): `lapse_risk` ⇐ `status === 'LAPSED'` (checked first, always wins); `renewal_inactivity` ⇐ the cycle has not concluded (`RENEWED`/`CANCELLED` excluded; `LAPSED` is NOT "concluded" — it's the other trigger) **and** `RENEWAL_INACTIVITY_THRESHOLD_BUSINESS_DAYS = 30` business days have elapsed since `triggeredAt`, reusing `isFollowUpDue` (`common/follow-up.util.ts` — the same test the RFQ #12 / Claim #27 follow-up sweeps use) — **DRAFTED/UNSOURCED**, same status as the #41/#42 SLA figures. **The race-safe invariant is the `RenewalCase.retentionEscalatedAt` status-conditional stamp** — `escalateAndCreateRetentionCase` stamps + creates the `RetentionCase` in **ONE `$transaction`** (a deliberate local exception to the no-`$transaction` convention, the `claim.repository.ts createNotification` shape), `WHERE retentionEscalatedAt IS NULL AND status NOT IN (RENEWED, CANCELLED)` (the `RfqInsurer.followUpAlertSentAt` shape, plus a `status` re-assertion it didn't need); a `null` return (0 rows) → `skippedConcurrent`, a distinct counter from `failed`; `RenewalCase.status` is NEVER written by the sweep, only checked. **`@code-reviewer` (mandatory — this IS the race-safe-invariants.md implementation) → CHANGES REQUESTED → resolved: 1 BLOCKER** (stamp + create were two separate writes — a `create` failure after a successful stamp permanently stranded the `RenewalCase` as "escalated" with no `RetentionCase`, and no future sweep would ever reconsider it; fixed by the `$transaction` above, so a failed create rolls the stamp back too) **+ 1 MAJOR** (the stamp's `where` originally re-asserted only `retentionEscalatedAt: null`, not `status` — a `RenewalCase` concluding between the sweep's load and the stamp could still open a spurious case for a customer who just renewed; fixed by the `status NOT IN (...)` re-assertion above) — both gaps were dormant (no real `RenewalCase` traffic exists yet) but would be live the day the renewal module lands, exactly why the review caught them now. **Endpoints** (all `retention-case.manage`): `POST /retention-cases` (manual open), `POST /retention-cases/sweep` (on-demand, before `:id` routes, counts only), `GET /retention-cases?customerId=&status=&reason=` + `/:id`, `POST /retention-cases/:id/close` (idempotent, no body — the model has no note field). Nightly `RetentionSweepScheduler` at **08:00 UTC** (after the 07:00 claim follow-up sweep), the `system@ibms.internal` account precedent, delegating to the same `runSweep` the on-demand endpoint calls. No index gap (no new columns added to index). `apps/web/` gains a **"Retention"** screen. **Deferred**: no per-customer dedup of open cases (two at-risk policies for one customer open two cases — the schema has no FK to dedupe against); no outcome/resolution field on close; no link from a `RetentionCase` back to the triggering `RenewalCase`; no auto-close when the renewal eventually succeeds; the inactivity threshold is drafted; the renewal module itself still doesn't exist, so the sweep has no real traffic yet. | Read `meta/context/customer-service-lifecycle.md` § "Customer Retention (Process 46)" — **Domain E is now the fully-built reference shape for the rest of the backlog** — "the race-safe guard is `RenewalCase.retentionEscalatedAt` (pre-existing!), a status-conditional stamp done in the SAME `$transaction` as the `RetentionCase` create (a review BLOCKER on the first non-transactional pass) with `status NOT IN (RENEWED, CANCELLED)` re-asserted in the same `where` (a review MAJOR) — `RenewalCase.status` is never written by the sweep", "`lapse_risk` (status LAPSED) always wins over `renewal_inactivity` (30 business days unresolved, reusing `isFollowUpDue`) — mutually exclusive by construction", "built ahead of its data source: the renewal module (Part 3.9) doesn't exist, so this is a logged no-op in normal running — the #29 Loss Ratio precedent, not a bug", and "no per-customer one-open-case dedup — two at-risk policies for the same customer legitimately open two cases" are all easy to get wrong. |
| 2026-09-04 | `meta/context/customer-service-lifecycle.md` gains a **Customer Feedback (Process 45)** section (filed via `/brain-gap` at `ibms-app` Part C #45 — the backlog line names only the model, no checkboxes). **`CustomerFeedback` (Part 4 core schema) pre-existed and needed NO widening** — `customerId`, `context`, `score`, `comments`, `submittedAt` already covered a satisfaction-survey log. **No migration.** `feedback.log` (`[SALES_RELATIONSHIP_OFFICER]`) was seeded in `a440c1b` — **no seed change** (149 perms), and there is **no separate read permission** — `feedback.log` covers create *and* read (the #41 / #44 shape). **Not a `WorkflowTransitionService` entity, no maker/checker, no `SlaTimer`** — a factual log, create + read only, the `Interaction` #10 shape (simpler than #41/#42/#44: no status, no derived fields). New `apps/api/src/modules/customer-service/feedback.{config,service,controller}.ts` + 2 DTOs + `repositories/feedback.repository.ts`, wired as the **4th `CustomerServiceModule` controller**. `context` restricted to the model's own three documented values (`post_issuance` / `post_claim` / `post_renewal`); `score` optional, bounded 1–5 — **DRAFTED / UNSOURCED** (Part 3.8 names no scale), same status as `CLAIM_LARGE_THRESHOLD_JOD` (#23). **Endpoints** (all `feedback.log`): `POST /feedback` (404 unknown customer), `GET /feedback?customerId=&context=` (book-wide, capped `FEEDBACK_READ_LIMIT = 5000`), `GET /feedback/:id`; `submittedAt` backdatable via `parseHistoricalInstant`. **`comments` carries the shared `NO_FULL_ACCOUNT_NUMBER` guard, same as #41/#42/#44** (a first pass omitted it, reasoning it was the CRM `Interaction.summary` shape — the `@code-reviewer` pass corrected this as a MAJOR: unlike an arbitrary CRM note, feedback `comments` is customer-typed text solicited *immediately after* a claim settlement / issuance / renewal, exactly when a dissatisfied customer is most likely to paste a full account number, and the field is book-wide readable). **`comments` IS still excluded from the `CREATE` audit `afterValue`** (ids + `context` + `score` + `submittedAt` only) — the one surviving divergence, the CRM `Interaction.summary` precedent (not #41/#42's verbatim-note precedent): a customer's own subjective reflection is treated more conservatively than an operational "what was done" note for the audit trail specifically (the input guard is unaffected by that distinction). No ownership-based read gating (`feedback.log` is the sole, single-role gate on both write and read). **`@code-reviewer` (mandatory — Confidential-tier customer commentary + a new `CREATE` audit-row shape) → CHANGES REQUESTED → resolved** (1 MAJOR — the missing guard, fixed; 2 MINORs — e2e now also proves a non-Sales GET is 403'd + score-boundary/guard-rejection/empty-comments tests added; 2 NITs — for the record: no index on `CustomerFeedback`, no truncation-warn test, both deferred). `apps/web/` gains a **"Feedback"** screen. **Deferred**: no link from a feedback row to the triggering `Policy`/`Claim`/`RenewalCase` (`context` is a label, not a FK); the 1–5 scale is drafted; no automatic survey trigger (always a manual `POST`); no duplicate-response detection; no CSAT-dashboard aggregation; no index on `CustomerFeedback` (a follow-up migration once volume exists). | Read `meta/context/customer-service-lifecycle.md` § "Customer Feedback (Process 45)" before touching #46 — "the model pre-existed, NO migration, NO seed change, `feedback.log` covers create AND read (no separate read perm)", "NOT a workflow entity, NO maker/checker, NO SlaTimer — simpler than every other Domain E item so far", "`context` is restricted to exactly `post_issuance`/`post_claim`/`post_renewal`", "`comments` DOES carry the `NO_FULL_ACCOUNT_NUMBER` guard (like #41/#42/#44) but is STILL excluded from the audit row — only the audit exclusion follows the CRM `Interaction.summary` precedent, not the input guard too", and "the 1–5 `score` bound is drafted/unsourced" are all easy to get wrong. |
| 2026-09-04 | `meta/context/customer-service-lifecycle.md` gains a **Customer Communication (Process 44)** section (filed via `/brain-gap` at `ibms-app` Part C #44). **The `CommunicationLog` model + the `communication.send` perm (`[SALES_RELATIONSHIP_OFFICER, PLACEMENT_TECHNICAL_OFFICER, CLAIMS_OFFICER, FINANCE_COLLECTIONS_OFFICER]`) pre-existed — NO seed change** (149 perms). The model is **shared with Process 12** (RFQ correspondence); **DISCRIMINATOR: `rfqId IS NULL` == a Process-44 customer-communication row**, and every Process-44 read filters it (a #12 id 404s on `GET /communications/:id`). **Migration `20260904120000` (43rd) only WIDENS**: `Customer.preferredContactChannel InteractionChannel?` (nullable — the recorded channel preference; "recorded language" already lives in `Customer.languagePreference`), `CommunicationLog.isMarketing Boolean @default(false)` + `CommunicationLog.consentRecordId String?` (nullable FK → `ConsentRecord`, `ON DELETE SET NULL`), `@@index([customerId])` → `@@index([customerId, sentAt])` + `@@index([consentRecordId])`. **`CommunicationLog` is NOT a `WorkflowTransitionService` entity, NO maker/checker, NO `SlaTimer`** — a factual send log (the `Interaction` #10 / #12 shape). New module code in `apps/api/src/modules/customer-service/` (`communication.{config,service,controller}.ts` + DTOs) + `repositories/communication.repository.ts`, wired as the 3rd `CustomerServiceModule` controller. **Endpoints** (all `communication.send`): **`POST /communications`** (`{ customerId, body, channel?, languageUsed?, isMarketing?, templateId?, subject?, sentAt? }` — creates at `direction: OUTBOUND`, `respectedConsent: true`); **`GET /communications?customerId=&channel=&isMarketing=&direction=`** (book-wide Process-44 list, newest-first, capped `COMMUNICATION_READ_LIMIT = 5000`); **`GET /communications/consent-status?customerId=`** (`{ customerId, marketing: { allowed, reason, consentRecordId } }` — declared BEFORE `:id`); **`GET /communications/:id`**. **"Respect the customer's recorded channel and language"** — both **derived, not an input** (`resolveChannel` / `resolveLanguage`, pure — the #28 / #31 / #38 "computed when derivable" rule): omit → taken from the `Customer` record; an explicit value that **disagrees** → **422**; `channel` is a required input (422) only when the customer has no `preferredContactChannel`. **The marketing-consent gate** (`evaluateMarketingConsent`, pure) runs only for `isMarketing: true` — loads the customer's `ConsentRecord` rows where `purpose = 'MARKETING' OR isMarketing = true`, takes the **most recent** by `grantedAt ?? createdAt`, `granted && withdrawnAt == null` ⇒ allowed (id stamped onto `consentRecordId`); otherwise **BLOCKED with a 422** (`reason` = `no_record` / `not_granted` / `withdrawn`), **no `CommunicationLog` row written**, and a best-effort **`REJECT`** audit row (`entityId: 'blocked'`, `afterValue` = `customerId` + `channel` + `blocked: 'marketing_consent_<reason>'`, no body/subject). A non-marketing send never touches the consent table. **`subject` / `body` carry the `NO_FULL_ACCOUNT_NUMBER` guard**, are Confidential-tier free text, and **never enter an audit row** (the `CREATE` `afterValue` is channel/language/consent metadata only — the #12 / CRM precedent); reads are **not** audited (Confidential tier — the #33 / #34 / #41 precedent). `sentAt` is backdatable (`parseHistoricalInstant`). `apps/web/` gains a **"Communications"** screen. **Deferred**: no real delivery integration (a log, not a sender); `isMarketing` is caller-asserted; the `POST` endpoint only writes `OUTBOUND`; no `CommunicationLog` → CRM 360° timeline wiring; no template render step; no bulk / campaign send; no `Customer` update endpoint so `preferredContactChannel` is set only at customer creation. | Read `meta/context/customer-service-lifecycle.md` § "Customer Communication (Process 44)" before touching #45–46 — "the `CommunicationLog` model + `communication.send` perm pre-existed, migration `20260904120000` only WIDENS, NO seed change", "the `rfqId IS NULL` discriminator separates Process-44 rows from Process-12 RFQ correspondence — every read filters it", "channel + language are DERIVED from the `Customer` record, a disagreeing explicit value is a 422 (`Customer.preferredContactChannel` is the new recorded-channel field)", "a marketing send (`isMarketing: true`) is BLOCKED with a 422 + a `REJECT` audit row + NO `CommunicationLog` row unless there is an *active* MARKETING `ConsentRecord` (`granted && withdrawnAt == null`) AND no withdrawal event is at least as recent as the newest active grant (the fail-safe multi-record rule — a withdrawal on a *different / older* record still blocks; the precedence is drafted pending a pinned `PRIV-SOP-04` section) — a non-marketing send skips the gate", "the gate is a read-then-write with NO DB constraint — fine only because this is a *log*, a real dispatch must re-check consent at send time", and "NOT a WorkflowTransition entity, NO maker/checker, NO SlaTimer — a factual log" are all easy to get wrong. |

---

## What this brain is for

**IBMS — Insurance Brokerage Management System.** A system for a licensed insurance/reinsurance broker operating in Jordan, covering the full loop from lead through claims and renewal, built to CBJ insurance regulation, Jordan PDPL No. 24/2023, and ISO/IEC 27001 + 27701.

This brain is seeded **before** a line of application code exists — from the approved Business & Technical Context Document and the already-approved Privacy Compliance Management System (PCMS) toolkit (1 policy, 4 standards, 10 procedures, 10 forms, 1 SRS), not from PR review history. That is a deliberate substitution of Input 1 in `INTAKE.md`: the "rules that would block a PR" already exist as signed-off regulatory documents. Once an engineering repo exists, add PR-derived lex the normal way and this note can go.

**Broker legal name:** not yet supplied to this brain — replace throughout once known. It does not block anything below.

---

## Staying current

Sync at session start — fetch, and pull only if behind **and** clean **and** on `main`. If dirty or on a feature branch, notify instead of pulling. Rule: `meta/lex/brain-freshness.md`.

---

## Repo map

```
meta/lex/         Mandatory rules. Read before non-trivial work.
meta/context/     How things actually work here. Read before touching an area.
meta/designs/     Why things are the way they are. Read before changing a decision.
meta/agents/      Agent definitions (source of truth).
meta/guides/      Advisory. Setup, onboarding, contributing.
meta/templates/   PR, ticket, and doc templates.
.claude/agents/   Mirror of meta/agents/ — what Claude Code actually loads.
.claude/hooks/    Enforcement scripts.
.claude/commands/ Slash commands.
```

---

## Modules (business view — not yet mapped to repos)

`ibms-app` exists as a single web+api monorepo — it is not yet split by module or service boundary. What exists is the module inventory from `PRIV-SRS-01` and the 74-process/8-domain inventory in the context document. Treat this as the module list, not a services table, until a service-boundary decision fills it in:

| Module | Governs |
|---|---|
| Core IBMS (Sales/CRM, Policy, Claims, Finance) | The 74 business processes — see `meta/context/policy-lifecycle.md` and `meta/context/claims-lifecycle.md` |
| M01–M12 (PCMS) | Privacy/compliance modules — see `meta/context/pcms-privacy-modules.md` |

## Architecture relationships

**IBMS and the PCMS (Privacy Compliance Management System) are one system, not two.** PCMS is the source of truth for every privacy/consent/retention/breach/DSR rule; IBMS's compliance module consumes PCMS decisions and feeds it data (customers, policies, claims) — it must never re-derive or duplicate a privacy rule. See `meta/designs/2026-08-pcms-source-of-truth.md`.

`ibms-app` is a Next.js (web) + NestJS (api) + PostgreSQL/Prisma monorepo. See `meta/designs/2026-08-ibms-app-stack-and-repo-split.md` for why, including the Prisma 6-vs-7 and repo-split calls. No call-direction / auth-boundary / system-of-record decision beyond "web calls api" has been made yet. **Do not invent one.** Record it here the day it's decided.

`ibms-app` vendors this repo as a pinned git submodule (`ibms-app/ibms-brain/`) rather than restating any rule — see `meta/designs/2026-08-ibms-app-brain-submodule-sync.md`. This repo has no reverse dependency on `ibms-app` and does not need one.

---

## Common commands

**This repo (`ibms-brain`) has none — it stays documentation-only.** For `ibms-app` (the engineering repo): `npm install`, `npm run dev`, `npm run test`, `npm run e2e` — see its own `README.md`/`CLAUDE.md`, not this file, for the full list and any changes to it.

## Environment

**This repo needs none.** `ibms-app` pins Node `20.13.0` (`.nvmrc`) and requires Docker — see its `README.md`. Record changes there, not here.

---

## Agents

| Agent | Use for |
|---|---|
| `@code-reviewer` | Review before push. **Mandatory** for any workflow/approval logic, financial (premium/commission/claim) calculation, or code touching Confidential/Highly Confidential data. |
| `@software-developer` | Implementation, bug fixes, refactoring. |

Definitions are source-of-truth in `meta/agents/` and mirrored to `.claude/agents/` by `.claude/hooks/mirror-agents.sh`. **Never hand-copy.**

---

## Mandatory rules

Loaded from `meta/lex/`. Enforcement level is stated in each file.

| Lex | Governs |
|---|---|
| `money-decimal-jod.md` | Decimal only for premium/commission/claim amounts |
| `workflow-state-transitions.md` | Never assign a workflow `status` directly — always through a transition function |
| `race-safe-invariants.md` | A "one of these / only once" invariant is a DB constraint or a status-conditional write, never a `findMany().find()` check-then-act |
| `maker-checker-segregation.md` | No self-approval on KYC, policy checking, refunds, disposal, DSR closure |
| `sensitive-data-handling.md` | Highly Confidential data (medical, financial, national ID, UBO) never logged/exported/shared unencrypted |
| `pdpl-sla-timers.md` | Every statutory SLA (consent withdrawal, DSR, breach containment, disposal) is a tracked deadline, not documentation |
| `backup-rpo-rto.md` | Encrypted backups + an actually-tested restore drill, not backup-only assurance |
| `kyc-aml-sla-timers.md` | KYC compliance-review turnaround + periodic re-KYC cadence are tracked deadlines (CBJ AML domain, not PDPL) — values currently draft/unsourced |
| `code-review.md` | Review format and severity levels |
| `definition-of-done.md` | No push without evidence from the verification contract |
| `workspace-updates.md` | Keeping this file and README current |
| `brain-freshness.md` | Session-start sync |

---

## What "working" means

`meta/context/verification-contract.md` defines the gates every change must pass and the evidence each produces. Run them with `bash scripts/verify.sh`. Claims are not evidence — exit codes and screenshots are. **In this repo, the only real gate is `bash scripts/brain-doctor.sh`** — it stays documentation-only. For `ibms-app` changes, real gates now exist: `npm run typecheck|lint|test|build|test:e2e|e2e` — see `meta/context/verification-contract.md` § Backend/frontend gate commands. Do not deploy to production; open a PR with evidence attached and let a human merge.

Path-scoped rules live in `.claude/rules/` and load themselves when you touch matching files — none exist yet because there is no code to scope them to.

## Domain glossary

See `meta/context/glossary.md`. Terms that mean something specific here — **account**, **customer**, **policy**, **loss ratio**, **maker/checker** — go there, not in this file.

---

## MR standards

**Not yet decided** — there is no engineering repo. Record branch naming, PR title format, and review rule here the day one is created. Until then, `meta/templates/pr-description.md` is the description format for any interim proposal/ticket writing.

---

## meta/ structure rules

1. Agent definitions are source-of-truth in `meta/agents/`. The mirror to `.claude/agents/` is automated — do not hand-copy.
2. Designs go in `meta/designs/<area>/` with a descriptive filename. No free-form naming.
3. A file belongs in `lex/` only if it has a filled "How it is enforced" section. Otherwise it goes in `guides/`.
4. Do not create a folder before it has content.
5. All cross-document links point to **this repo**. The PCMS documents (`PRIV-STD-*`, `PRIV-SOP-*`, `PRIV-FRM-*`, `PRIV-SRS-*`) are the canonical location for privacy/compliance detail — this brain cross-references them, it never restates them as a second source of truth.

---

## Mindset

Research before architecture. Architecture before execution.

`context/` exists so nothing has to be explained twice — to a human or to an agent.

The brain grows by use. When an agent asks something this repo should have answered, that gap is the next file. Run `/brain-gap`.
