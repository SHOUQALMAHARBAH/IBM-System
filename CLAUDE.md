@AGENTS.md

# CLAUDE.md — ibms-brain

Workspace instructions. Loaded into every Claude Code session in this repo and in every engineering repo that syncs the brain, once those repos exist.

<!-- The @AGENTS.md line above is an import, not a link. Claude Code reads CLAUDE.md and
     NOT AGENTS.md, so without that import the AGENTS.md file is dead weight for Claude
     while still being read by other agent tools. Keep the import on line 1.
     Target for this file: under 200 lines. Longer files reduce adherence. -->

---

## What's New

| Date | Change | Action required |
|------|--------|-----------------|
| 2026-09-20 | **NEW `meta/context/vendor-management.md`** (Process 71 — M07) — filed via `/brain-gap` at `ibms-app` (backlog Part C #71, Domain H). Three checkboxes: risk tiering (low/medium/high) before any data share or access grant; a mandatory Data Processing Agreement for Medium/High tier + DPO approval for High tier before the first share; mandatory annual review + confirmation of data return/destruction on termination + access revocation within 2 business days. #67 (Procurement) built the FOUNDATIONAL `Vendor` CRUD and explicitly deferred `riskTier`/`annualReviewDueAt`/`terminationDataReturnConfirmedAt`/`accessRevokedAt` to this process — SAME model, SAME module, extended not duplicated. **Checkbox 1 has no live call site to enforce against** — `DataSharingApproval` (M08) has zero prior writer (flagged `dormant: true`, same as `DataProcessingAgreement` was before this process) and no model represents a generic "access grant" at all; built `computeDataShareReadiness()` as a pure, queryable rule (`GET /vendors/:id/data-share-readiness`) instead of inventing the M08 workflow — an honest gate with NO live enforcement call site yet. **`DataProcessingAgreement`'s maker/checker pair (`assessedByUserId`/`dpoApprovedByUserId`) already had a DB `CHECK` constraint and a covered-pairs table entry since the A.5 foundational work, zero prior writer** — this process is that first real writer; `dpa.approve` (DPO only) was already pre-seeded as a DISTINCT permission code from `vendor.manage` — the maker-checker default (two codes), not a shared-permission exception. **Deletion/dual-control confusion avoided**: the Department-Manager-+-DPO dual control `maker-checker-segregation.md` describes is the M06 Disposal `DisposalBatch` BATCH-destruction workflow (a separate, still-unbuilt process) — NOT this process's own, already-distinct DPA maker/checker pair. **`vendor_termination_access_revocation` (2 business days) needed a genuinely NEW SLA registry entry** — unlike every other Domain G/H SLA this session found dormant-but-pre-seeded (including this SAME process's own `vendor_annual_review`), `pdpl-sla-timers.md`'s lex table had NO row for vendor termination access revocation at all; added to both the lex table and `sla-registry.config.ts`, sourced directly from the backlog's own "2 business days" text (the M03 sourcing precedent). **`Vendor` has no explicit "terminated" status** — termination is `terminationDataReturnConfirmedAt` going null-to-set, gated by a mandatory explicit `confirmDataReturnOrDestruction: true` attestation (the `FulfilDsrDto` precedent) since there's no automated data-return verification; `accessRevokedAt` is a separate, subsequent stamp requiring termination first (422 otherwise), both independently status-conditional (409 on double-execution). Risk tiering auto-schedules the annual-review SLA on the FIRST Medium/High tiering only (a later downgrade doesn't un-schedule it); `recordAnnualReview()` re-bases +12 months via the documented resolve-then-start-new SLA re-basing shape. No new permission, no migration — `vendor.manage`/`dpa.approve` were both pre-seeded. `apps/web/` gains a **vendor detail screen** (`app/(app)/vendors/[id]/page.tsx` — risk tiering, readiness check, DPA lifecycle, termination/revocation); the list page gains a "Manage" link into it. **Verification**: +42 api unit (`vendor.config.spec.ts` 13, `vendor.service.spec.ts` +27/-0 net rewritten, `data-processing-agreement.service.spec.ts` 10, plus an `sla-registry.config.spec.ts` count-assertion update) → api unit **2086** (154 files, from 2044). Extended `test/vendor.e2e-spec.ts` to **8/8** (from 4) — permission gating unchanged; a full risk-tiering→annual-review→re-review walk; the full DPA maker/checker lifecycle + readiness gate for a High-tier vendor (403 self-approval, 409 double-approval); Low-tier readiness with no DPA; the termination→revocation walk with order-guarding and double-execution 409s. Full api unit suite 2086/2086 confirmed green; full 48-file api e2e suite green across 8 foreground sub-batches, both chronic flakes (`rbac`, `up-sell`) passing with `--testTimeout=90000`. New Playwright `vendor-detail.spec.ts` 5/5; full Playwright suite 232/232 (from 227). `npm run typecheck`/`lint`/`build` (api + web) OK — one real a11y catch (an unlabeled `<select>` for risk tier), fixed with a wrapping `<label>`. | Read `meta/context/vendor-management.md` before touching #71 or any future process describing a rule with "before any X" phrasing when X has no existing model/action — "a policy statement naming an action that doesn't exist yet in the codebase (no model, no writer) should become a pure, queryable readiness/gate FUNCTION for a future consumer to call, not an excuse to invent the missing workflow yourself", "two lex files (`roles-and-segregation-of-duties.md`, `maker-checker-segregation.md`) can BOTH describe a dual-control requirement for something that sounds like your current process but is actually a DIFFERENT, larger, separately-named process (M06 Disposal vs. this process's own DPA maker/checker) — read the cited Part/SOP number, not just the word 'dual control', before concluding your process needs two permission codes it doesn't", "a dormant-but-pre-seeded SLA registry entry is common in this codebase by now, but check EVERY SLA line a process's backlog names individually — even a process with ONE already-pre-seeded SLA entry (`vendor_annual_review`) can have a SECOND, completely absent one (`vendor_termination_access_revocation`) sitting right next to it", and "an entity's own 'terminated' state can be entirely implicit (a field going null-to-set) with no enum — don't assume a status field is missing by accident before checking whether the null-to-set field IS the intended state machine, the #66 `Employee.terminationDate` shape repeated" are all easy to get wrong. |
| 2026-09-20 | **NEW `meta/context/document-management.md`** (Process 70) — filed via `/brain-gap` at `ibms-app` (backlog Part C #70, Domain H). Two checkboxes: full version control (previous/next version) + mandatory classification + a deletion lock disabled by default except via a logged privileged override (`deletionOverrideByUserId`); and enforcing the "highest classification present" rule when a file/record mixes classification levels. `Document` (Part 4.2, the electronic Insurance File) pre-exists with THREE prior writers (Policy issuance/attach #18-19, Claim documentation #25, Customer onboarding #3-4), each only ever creating a version-1 row — `versionNumber`/`previousVersionId` and `deletionLocked`/`deletionOverrideByUserId` had sat dormant since before this process; `audit-trail.repository.ts`'s own `findDocumentVersionChain` doc comment already said "no application code creates a second version yet" — a READ-side capability (#57's External Auditor lens) had existed for LONGER than any writer. #70 is that first real writer. **Version control mirrors `QuotationService.revise` exactly**: `id` must be the chain's own current (leaf) version — a superseded id 422s rather than silently resolving to the real leaf; a genuine concurrent double-version loses `previousVersionId`'s own `@unique` constraint and 409s. A new version's `category`/`policyId`/`customerId` are inherited from its predecessor, never accepted from the caller — only `fileName`/`storageRef`/`classification` can change per version, `classification` staying mandatory on every version (not just the first). **Deletion is a single-actor privileged override, NOT the M06 Disposal dual control** — `roles-and-segregation-of-duties.md` requires Department Manager + DPO dual control for the *disposal BATCH* destruction decision (`DisposalBatch`, a separate, still-unbuilt process, flagged `dormant: true` in `internal-controls.config.ts`'s `MAKER_CHECKER_REGISTRY`); #70's own singular field and its pre-seeded single-code permission (`document.delete-override`, `[SYSTEM_SECURITY_ADMINISTRATOR, DATA_PROTECTION_OFFICER]`) both anticipate the narrower, ad hoc, single-document shape instead — confirmed by checking the actual seed data before assuming a maker/checker pair was needed. **A document can only be deleted from the leaf of its own chain — an APPLICATION-level guard, not a DB one**: `Document_previousVersionId_fkey` is `ON DELETE SET NULL`, not `RESTRICT` — Postgres will not stop a delete that orphans a successor, it silently nulls the successor's `previousVersionId` instead; `hasSuccessor()` must be checked before every delete, a documented, deliberate limitation (hardening the FK to `RESTRICT` was considered and deferred, not asked for by #70's own checkboxes). A document still linked to a `ClaimDocument` also cannot be deleted — here the DB's `ON DELETE RESTRICT` on `ClaimDocument.documentId` IS a genuine backstop behind the same app-level pre-check. `overrideDeletionLock`/`deleteIfUnlocked` are both status-conditional writes (`updateMany`/`deleteMany` re-asserting `deletionLocked` in the `where`), never a `findUnique` + unconditional write. **The "highest classification present" rollup is scoped to `Document.policyId` only** (`GET /documents/classification-summary?policyId=`) — matching the schema's own "every Policy resolves to ONE electronic Insurance File" framing; a claim-linked document (joined via `ClaimDocument`, never carrying its own `policyId`) is deliberately out of scope. The rule itself is `sensitive-data-handling.md` / `PRIV-STD-02` §6.7 verbatim: "a file combining multiple classification levels is classified at the highest level present — never averaged." **`#70`'s new `document.manage` routes deliberately do NOT duplicate `#57`'s existing `GET /audit-trail/documents/:id/history`** (`document-history.read`, COMPLIANCE/AUDITOR only, a compliance-forensics lens) — the people who actually upload/version documents mostly cannot reach that route at all; #70's own `GET /documents`/`GET /documents/:id` are a plain operational browse instead, a non-overlapping addition. No new permission, no migration — `document.manage`/`document.delete-override` were both pre-seeded (Domain H's "seed before code" pattern holding again after #69 broke it). **Verification**: +21 api unit (`document.config.spec.ts` 6, `document.service.spec.ts` 15) → api unit **2044** (152 files, from 2023). New `test/document.e2e-spec.ts` **6/6** — permission gating (`document.manage` vs `document.delete-override` checked separately); create-version → 422 on a superseded id; the full lock/unlock/delete lifecycle (409 while locked, unlock, a second override 409s, delete succeeds, row confirmed gone via a direct Prisma read since the deleting ADMIN role holds no read permission); 409 deleting a document with a newer version even once unlocked; 409 deleting a document still attached to a claim; the classification-summary rollup + a 404 for an unknown policy. Full api unit suite 2044/2044 confirmed green; full 48-file api e2e suite green across 8 foreground sub-batches — one confirmed-transient environment hiccup this run (an unrelated file, re-confirmed clean on isolated re-run) and one confirmed-transient TOTP-timing flake (also unrelated, re-confirmed clean on isolated re-run), both chronic flakes (`rbac`, `up-sell`) passing with `--testTimeout=90000`. New Playwright `documents.spec.ts` 5/5; full Playwright suite 227/227 (from 222). `npm run typecheck`/`lint`/`build` (api + web) OK — two real lint catches: unescaped apostrophes (`react/no-unescaped-entities`) in the new web page's prose, and a TypeScript literal-type inference gap in the new Playwright spec's mutable fixture object, both fixed. | Read `meta/context/document-management.md` before touching #70 or any future "version control"/"deletion lock override" feature — "check for a DORMANT read-side capability before assuming a write-side feature needs new design work from scratch — `audit-trail.repository.ts`'s version-chain walker existed and was documented as having zero writers well before this process built the first one", "a 'privileged override' field described in the singular is not automatically a maker/checker pair — check the ACTUAL pre-seeded permission's role list and the schema's own field count before retrofitting dual control onto something that doesn't ask for it, and cross-reference `roles-and-segregation-of-duties.md`/`maker-checker-segregation.md` for a SEPARATE, already-named batch-level process (M06 Disposal) that the singular field might be getting confused with", "a foreign key's actual `onDelete` behavior in the migration SQL can differ from what you'd assume from the schema alone (`SET NULL` here, not `RESTRICT`) — check the migration file directly before relying on the DB to enforce a referential-integrity invariant your application logic assumes it does", and "a new module's routes should be checked against an EXISTING module's routes for the same underlying entity before assuming a capability needs building from scratch — #57's audit-trail lens and #70's own operational browse serve different audiences over the same table, deliberately not merged" are all easy to get wrong. |
| 2026-09-19 | **NEW `meta/context/information-asset-inventory.md`** (Process 69) — filed via `/brain-gap` at `ibms-app` (backlog Part C #69, Domain H). The backlog's own annotation: "fully covered by Part A + `IncidentReport` + `InformationAsset`." Verified rather than accepted — the #68 discipline applied again, and this time the claim was OVERSTATED more than #68's case. **Part A's cybersecurity-relevant items (A.1 Auth/Sessions, A.3 Encryption/Key Management, A.4 Immutable Audit Trail, A.9 Data Masking, A.10 Infra/Deployment) are real but each carries its own already-tracked gap** (hardware-token MFA, a real KMS/HSM, encryption-at-rest, unwired `assertSecureChannel`/`assertExportAllowed` callers, Dev/Test/UAT/Prod separation "scaffolded, not achieved") — all already documented in `ibms-app`'s own README § Known gaps, none restated. **`IncidentReport`** (built for #55/M09) is genuinely real — its own doc comment already frames it as a "unified security + personal-data breach workflow" — but carries no dedicated cyber/category field, just free-text `title`/`description` + a `severity` string. **`InformationAsset`** (ISO 27001 Clause 8.1 asset inventory) was the one genuine gap: **completely dormant, zero prior application code anywhere** — the SAME "dormant model, first real writer" shape #58-67 repeatedly found. This process built its foundational CRUD: create/list(filtered by `assetType`/`classification`)/get/update on the model's own four fields (`name`, `assetType`, `ownerUserId`, `classification`) — the #67 `Vendor` "minimal CRUD only" scope. `assetType` has no DB enum but is validated app-side against the model's own doc-comment 6-value set (`ASSET_TYPES` in `information-asset.config.ts`); `ownerUserId` is a bare scalar with no Prisma relation (the `Opportunity.createdByUserId` shape), validated against a real `User` via `UserRepository.findById()` in the SERVICE layer (404 if not found) — the #66 `Employee.userId` link-validation precedent. **This is Domain H's FIRST item to break the "seed before code" pattern** — #66/#67 both found their permissions already pre-seeded; `information-asset.manage` (`[SYSTEM_SECURITY_ADMINISTRATOR, COMPLIANCE_OFFICER]`) had no pre-seeded grant, requiring a genuine seed-data change and a re-seed of both dev and test databases (151 → 152 permissions). `apps/web/` gains an **"Information Assets"** screen (list + create + inline rename, the `vendors` page shape). **Verification**: +9 api unit (`information-asset.service.spec.ts`) → api unit **2023** (150 files, from 2014). New `test/information-asset.e2e-spec.ts` **7/7** — permission gating; a 400 rejecting an `assetType` outside the documented 6-value set; a 400 rejecting a `classification` outside the `DataClassification` enum; a 404 creating an asset owned by a non-existent user; a real create→list(filtered)→get→update walk; a 404 reassigning an asset to a non-existent owner on update; 404s for an unknown asset on both GET and PATCH. Full api unit suite 2023/2023 confirmed green; full 47-file api e2e suite green across 8 foreground sub-batches, both chronic flakes (`rbac`, `up-sell`) passing with `--testTimeout=90000`. New Playwright `information-assets.spec.ts` 4/4; full Playwright suite 222/222 (from 218). `npm run typecheck`/`lint`/`build` (api + web) OK. | Read `meta/context/information-asset-inventory.md` before touching #69 or any future backlog line whose OWN annotation claims "fully covered by" a list of existing pieces — "verify EACH named piece independently rather than accepting the backlog's own coverage claim at face value — the #68 discipline, and #69 proves it isn't a one-off: a claim can be MORE overstated the second time a session checks one", "a model can be 'covered' in name only — `InformationAsset` existed in the schema but had zero prior consumers, the same dormant-model shape #58-67 kept finding, and the backlog's own citation of a model's existence is not evidence anyone ever wrote to it", and "Domain H's 'permission already pre-seeded' pattern (#66, #67) is not a guarantee — #69 needed a genuine new seed-data change, so check the seed file directly rather than assuming the pattern holds for every remaining #70-74 item" are all easy to get wrong. |
| 2026-09-18 | **NEW `meta/context/procurement.md`** (Process 67) — filed via `/brain-gap` at `ibms-app` (backlog Part C #67, Domain H). The backlog's own text carries an explicit scope warning, quoted in full since it IS the design brief: "The two source documents give no more than a one-line general description ('purchase requests and vendor selection for non-insurance operational needs') — no field-level detail or defined workflow in the source. The only task actually executable from the source directly: use `Vendor` (with `vendorType=other`) as the general vendor record for this purpose, without inventing a purchase-request model that isn't in the text." **No `PurchaseRequest` model, no approval workflow, nothing beyond the literal instruction was built** — the opposite case from most backlog items: the source names EXACTLY which existing model to reuse and exactly what NOT to invent, rather than leaving a gap to map. **`Vendor` is a genuinely shared register — THREE consumers, one model**: its own schema doc comment already says "Merges Process 71 (Vendor Management) and PDPL third-party governance — both describe the same register"; #67 adds a third consumer (Procurement, `vendorType: 'other'`) to the SAME rows #71 (risk tiering/DPAs/annual review) and Part D's Third-Party & Data Sharing section will also read/write. `Vendor` had zero prior application code — the same "dormant model, first real writer" shape #58-66 repeatedly found. **What #67 built vs. what stays #71's job**: `VendorRepository`/`VendorService`/`VendorController` implement ONLY the foundational CRUD (create/list/get/update on name+vendorType) — `riskTier`/`annualReviewDueAt`/`terminationDataReturnConfirmedAt`/`accessRevokedAt` all exist on the model already but are DELIBERATELY untouched by any #67 code path; #71, when built, extends the SAME `VendorModule`/`VendorRepository` rather than creating a second, parallel Vendor CRUD. `vendorType` is validated against the exact 7-value set from the schema's own doc comment (`VENDOR_TYPES` in `vendor.config.ts`) so a caller can't write an arbitrary string into a column with no DB-level enum. **No new permission** — `vendor.manage` (`[COMPLIANCE_OFFICER, BRANCH_DEPARTMENT_MANAGER, SYSTEM_SECURITY_ADMINISTRATOR]`) was already pre-seeded, its own seed description already reading "Manage a vendor record AND ITS RISK TIER" — anticipating #71's extension to the SAME permission, not a new one. `apps/web/` gains a **"Vendors"** screen (a single list+create+inline-rename page — no separate detail route, since nothing rich exists per vendor yet). **Verification**: +8 api unit (`vendor.service.spec.ts`) → api unit **2014** (149 files, from 2006). New `test/vendor.e2e-spec.ts` **4/4** — permission gating; a 400 rejecting a `vendorType` outside the documented 7-value set; a real create→list(filtered)→get→update walk for a procurement vendor (a name-only PATCH leaves `vendorType` untouched); 404s for an unknown vendor on both GET and PATCH. Full api unit suite 2014/2014 confirmed green; full 46-file api e2e suite green across 8 foreground sub-batches — a clean run this time, no transient flakes, both chronic flakes (`rbac`, `up-sell`) passing with `--testTimeout=90000`. New Playwright `vendors.spec.ts` 4/4; full Playwright suite 218/218. `npm run typecheck`/`lint`/`build` (api + web) OK — no lint findings this time. | Read `meta/context/procurement.md` before touching #67 or #71, or any future backlog item whose OWN text names the exact existing model to reuse — "when the source material explicitly says which model to reuse and what NOT to invent, follow that instruction literally rather than defaulting to this session's usual 'map the vague requirement to the closest existing signal' judgment call — there is no judgment call to make here", "a model can be shared across THREE separate backlog processes at once (#67/#71/Part D), each populating a different subset of its optional fields — check a model's own schema doc comment for 'merges Process N and M' before assuming a new process needs its own table", and "when a later backlog item (#71) will clearly extend the SAME model/module a smaller earlier item (#67) builds, keep the earlier item's CRUD genuinely minimal and leave the later item's own fields completely untouched, rather than guessing at #71's future business rules now" are all easy to get wrong. |
| 2026-09-17 | **NEW `meta/context/employee-onboarding.md`** (Process 66) — filed via `/brain-gap` at `ibms-app` (backlog Part C #66, Domain H — **opens Domain H, Supporting Operations #66-74**). Two checkboxes: an employee record + licensing/certification tracking for regulated staff + training records; an automated access de-provisioning checklist on an employment-status change (same business day). `Employee`, `SecurityAwarenessTraining`, `AccessDeprovisioningChecklist` all pre-exist in the core schema (Part 8.2) with zero prior application code — the exact "dormant model, first real writer" shape #58-65 repeatedly found in Domain G. **Licensing/certification tracking maps to existing flat fields** — `Employee.licensedRole`/`confidentialityAgreementSignedAt`/`backgroundCheckCompletedAt` ARE the schema's complete design for this; no separate license/certificate table was invented. **`terminate()` is the first real caller of an ALREADY-REGISTERED SLA** — `pdpl-sla-timers.md`'s own registry already sources "Termination access revocation (M05) | Same business day | Critical alert to IT management if still open after 24h," already transcribed in `SLA_REGISTRY` as `termination_access_revocation` (`duration: 0 hours`, one `+24h -> 'IT_MANAGEMENT'` escalation stage) with zero prior caller — no new SLA design work was needed, only wiring `SlaTimerService.startTimer()`/`.resolve()` to the real events. **Termination is a stamp+create transaction, not a status enum** — `Employee` has no `EmploymentStatus` field; `terminationDate` going `null` -> a date IS the "employment-status change." `EmployeeRepository.terminate()` stamps `terminationDate` and creates the checklist in ONE `$transaction` (the `retention-case.repository.ts#escalateAndCreateRetentionCase` shape — a precedented local exception to the no-`$transaction` convention), the stamp's `where` re-asserting `terminationDate: null` so a concurrent second termination gets a clean 409, not a race. **`systemAccessRevoked` has a REAL effect, not just a timestamp**: ticking it also sets the linked `User.isActive = false` and calls `SessionService.revokeAllForUser(userId, 'admin_revoked')` — killing every live session immediately, proven end-to-end (a real bearer token 401s the instant it fires), since a checklist box with no actual access-control effect would satisfy Part 8.2's letter while missing its point entirely (PRIV-STD-02, PRIV-SOP-01/02/03). **`User.employeeId` — #61's dormant FK — gets its first real writer too**: `POST /employees` accepts an optional `userId`, verified not-already-linked (409) before linking, a deliberate minimal scope addition beyond the backlog's own two checkboxes that finally makes #61's `EmployeePerformanceRecord` usable. **Permission split**: `employee.manage` (`[ADMIN, MANAGER]`) gates the general record/training surface; `deprovisioning.execute` (`[ADMIN]` ONLY, narrower) gates BOTH `terminate` and every checklist action — terminating IS the trigger, so it sits behind the SAME narrow permission as executing the checklist, not the broader one a Manager also holds. Zero seed change — both pre-seeded ahead of time, the Domain G "seed before code" pattern extended to Domain H. **Encryption mirrors `CustomerService` field-for-field** — `Employee.nationalIdEnc` was already registered in `ENCRYPTED_FIELDS` with zero prior consumer; masked-by-default, justified reveal via `POST /employees/:id/reveal-field`, list view strips the encrypted field entirely rather than decrypt-then-mask N times. `apps/web/` gains **"Employees"** (list+create) and an `[id]` detail screen (profile+reveal, training, the de-provisioning checklist). **Verification**: +31 api unit (`employee.config.spec.ts` 6, `employee.service.spec.ts` 25) → api unit **2006** (148 files, from 1975). New `test/employee.e2e-spec.ts` **3/3** — permission gating (Manager holds `employee.manage` but NOT `deprovisioning.execute`); a REAL full-lifecycle walk (create with a linked User -> reveal -> assign+complete training -> terminate -> verify the SLA timer row exists with `escalatedTo: 'IT_MANAGEMENT'` -> a second termination 409s -> completing the checklist early 400s -> ticking `systemAccessRevoked` deactivates the linked user AND 401s their live bearer token -> ticking the rest + completing resolves the SLA timer); a 409 rejecting a userId already linked elsewhere. Full api unit suite 2006/2006 confirmed green; full 45-file api e2e suite green across 8 foreground sub-batches — two confirmed-transient environment hiccups this run (both unrelated files, re-confirmed clean on isolated re-run), both chronic flakes (`rbac`, `up-sell`) passing with `--testTimeout=90000`. New Playwright `employees.spec.ts` 4/4; full Playwright suite 214/214. `npm run typecheck`/`lint`/`build` (api + web) OK — a second occurrence of the same `@typescript-eslint/no-unsafe-assignment` pattern from #65 (an asymmetric Vitest matcher nested inside an object literal argument), fixed the same way. | Read `meta/context/employee-onboarding.md` before touching #66-74 or any future "de-provisioning"/access-revocation feature — "check `SLA_REGISTRY` (`sla-registry.config.ts`) for an already-transcribed entry before designing a new SLA — `termination_access_revocation` was fully specified with zero prior caller, exactly like #60/#61's dormant models", "an entity with no status enum can still have a real, race-safe 'state change' — a nullable date field going null-to-set IS the transition; guard it with the stamp+create-in-one-transaction shape (`retention-case.repository.ts`), not a status-conditional update alone, when a SECOND row must be created atomically with the stamp", "a de-provisioning/access-revocation checklist item needs to actually revoke something (deactivate the account, kill the session) — a timestamp-only implementation satisfies the letter of the control while missing its entire regulatory point", and "an asymmetric Vitest matcher (`expect.any`/`expect.stringMatching`/etc.) nested INSIDE an object literal passed to `toHaveBeenCalledWith` trips `@typescript-eslint/no-unsafe-assignment` in this codebase's lint config — capture the mock call argument and assert on it directly instead, now a twice-confirmed pattern" are all easy to get wrong. |

---

## What this brain is for

**IBMS — Insurance Brokerage Management System.** A system for a licensed insurance/reinsurance broker operating in Jordan, covering the full loop from lead through claims and renewal, built to CBJ insurance regulation, Jordan PDPL No. 24/2023, and ISO/IEC 27001 + 27701.

This brain is seeded **before** a line of application code exists — from the approved Business & Technical Context Document and the already-approved Privacy Compliance Management System (PCMS) toolkit (1 policy, 4 standards, 10 procedures, 10 forms, 1 SRS), not from PR review history. That is a deliberate substitution of Input 1 in `INTAKE.md`: the "rules that would block a PR" already exist as signed-off regulatory documents. Once an engineering repo exists, add PR-derived lex the normal way and this note can go.

**Broker legal name:** not yet supplied to this brain — replace throughout once known. It does not block anything below.

---

## Staying current

Sync at session start — fetch, and pull only if behind **and** clean **and** on `main`. If dirty or on a feature branch, notify instead of pulling. Rule: `meta/lex/brain-freshness.md`.

---

## Repo map

```
meta/lex/         Mandatory rules. Read before non-trivial work.
meta/context/     How things actually work here. Read before touching an area.
meta/designs/     Why things are the way they are. Read before changing a decision.
meta/agents/      Agent definitions (source of truth).
meta/guides/      Advisory. Setup, onboarding, contributing.
meta/templates/   PR, ticket, and doc templates.
.claude/agents/   Mirror of meta/agents/ — what Claude Code actually loads.
.claude/hooks/    Enforcement scripts.
.claude/commands/ Slash commands.
```

---

## Modules (business view — not yet mapped to repos)

`ibms-app` exists as a single web+api monorepo — it is not yet split by module or service boundary. What exists is the module inventory from `PRIV-SRS-01` and the 74-process/8-domain inventory in the context document. Treat this as the module list, not a services table, until a service-boundary decision fills it in:

| Module | Governs |
|---|---|
| Core IBMS (Sales/CRM, Policy, Claims, Finance) | The 74 business processes — see `meta/context/policy-lifecycle.md` and `meta/context/claims-lifecycle.md` |
| M01–M12 (PCMS) | Privacy/compliance modules — see `meta/context/pcms-privacy-modules.md` |

## Architecture relationships

**IBMS and the PCMS (Privacy Compliance Management System) are one system, not two.** PCMS is the source of truth for every privacy/consent/retention/breach/DSR rule; IBMS's compliance module consumes PCMS decisions and feeds it data (customers, policies, claims) — it must never re-derive or duplicate a privacy rule. See `meta/designs/2026-08-pcms-source-of-truth.md`.

`ibms-app` is a Next.js (web) + NestJS (api) + PostgreSQL/Prisma monorepo. See `meta/designs/2026-08-ibms-app-stack-and-repo-split.md` for why, including the Prisma 6-vs-7 and repo-split calls. No call-direction / auth-boundary / system-of-record decision beyond "web calls api" has been made yet. **Do not invent one.** Record it here the day it's decided.

`ibms-app` vendors this repo as a pinned git submodule (`ibms-app/ibms-brain/`) rather than restating any rule — see `meta/designs/2026-08-ibms-app-brain-submodule-sync.md`. This repo has no reverse dependency on `ibms-app` and does not need one.

---

## Common commands

**This repo (`ibms-brain`) has none — it stays documentation-only.** For `ibms-app` (the engineering repo): `npm install`, `npm run dev`, `npm run test`, `npm run e2e` — see its own `README.md`/`CLAUDE.md`, not this file, for the full list and any changes to it.

## Environment

**This repo needs none.** `ibms-app` pins Node `20.13.0` (`.nvmrc`) and requires Docker — see its `README.md`. Record changes there, not here.

---

## Agents

| Agent | Use for |
|---|---|
| `@code-reviewer` | Review before push. **Mandatory** for any workflow/approval logic, financial (premium/commission/claim) calculation, or code touching Confidential/Highly Confidential data. |
| `@software-developer` | Implementation, bug fixes, refactoring. |

Definitions are source-of-truth in `meta/agents/` and mirrored to `.claude/agents/` by `.claude/hooks/mirror-agents.sh`. **Never hand-copy.**

---

## Mandatory rules

Loaded from `meta/lex/`. Enforcement level is stated in each file.

| Lex | Governs |
|---|---|
| `money-decimal-jod.md` | Decimal only for premium/commission/claim amounts |
| `workflow-state-transitions.md` | Never assign a workflow `status` directly — always through a transition function |
| `race-safe-invariants.md` | A "one of these / only once" invariant is a DB constraint or a status-conditional write, never a `findMany().find()` check-then-act |
| `maker-checker-segregation.md` | No self-approval on KYC, policy checking, refunds, disposal, DSR closure |
| `sensitive-data-handling.md` | Highly Confidential data (medical, financial, national ID, UBO) never logged/exported/shared unencrypted |
| `pdpl-sla-timers.md` | Every statutory SLA (consent withdrawal, DSR, breach containment, disposal) is a tracked deadline, not documentation |
| `backup-rpo-rto.md` | Encrypted backups + an actually-tested restore drill, not backup-only assurance |
| `kyc-aml-sla-timers.md` | KYC compliance-review turnaround + periodic re-KYC cadence are tracked deadlines (CBJ AML domain, not PDPL) — values currently draft/unsourced |
| `code-review.md` | Review format and severity levels |
| `definition-of-done.md` | No push without evidence from the verification contract |
| `workspace-updates.md` | Keeping this file and README current |
| `brain-freshness.md` | Session-start sync |

---

## What "working" means

`meta/context/verification-contract.md` defines the gates every change must pass and the evidence each produces. Run them with `bash scripts/verify.sh`. Claims are not evidence — exit codes and screenshots are. **In this repo, the only real gate is `bash scripts/brain-doctor.sh`** — it stays documentation-only. For `ibms-app` changes, real gates now exist: `npm run typecheck|lint|test|build|test:e2e|e2e` — see `meta/context/verification-contract.md` § Backend/frontend gate commands. Do not deploy to production; open a PR with evidence attached and let a human merge.

Path-scoped rules live in `.claude/rules/` and load themselves when you touch matching files — none exist yet because there is no code to scope them to.

## Domain glossary

See `meta/context/glossary.md`. Terms that mean something specific here — **account**, **customer**, **policy**, **loss ratio**, **maker/checker** — go there, not in this file.

---

## MR standards

**Not yet decided** — there is no engineering repo. Record branch naming, PR title format, and review rule here the day one is created. Until then, `meta/templates/pr-description.md` is the description format for any interim proposal/ticket writing.

---

## meta/ structure rules

1. Agent definitions are source-of-truth in `meta/agents/`. The mirror to `.claude/agents/` is automated — do not hand-copy.
2. Designs go in `meta/designs/<area>/` with a descriptive filename. No free-form naming.
3. A file belongs in `lex/` only if it has a filled "How it is enforced" section. Otherwise it goes in `guides/`.
4. Do not create a folder before it has content.
5. All cross-document links point to **this repo**. The PCMS documents (`PRIV-STD-*`, `PRIV-SOP-*`, `PRIV-FRM-*`, `PRIV-SRS-*`) are the canonical location for privacy/compliance detail — this brain cross-references them, it never restates them as a second source of truth.

---

## Mindset

Research before architecture. Architecture before execution.

`context/` exists so nothing has to be explained twice — to a human or to an agent.

The brain grows by use. When an agent asks something this repo should have answered, that gap is the next file. Run `/brain-gap`.
