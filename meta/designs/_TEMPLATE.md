# Design: <Title>

**Status:** `<proposed | accepted | superseded by <link>>`
**Date:** `<YYYY-MM-DD>` · **Author:** `<name>` · **Service:** `<service>`

## Context

<What situation forced a decision. Constraints that were real at the time — deadline, team size, existing system, cost.>

## Decision

<What was decided. One paragraph, stated plainly.>

## Alternatives considered

| Option | Why it lost |
|---|---|
| `<option>` | `<reason>` |
| `<option>` | `<reason>` |

> **This table is what makes it a design doc rather than a changelog.** Without it, the decision gets re-litigated in six months by someone who assumes the alternatives were never considered.

## Consequences

**Accepted costs:** <what this makes harder or slower.>

**Revisit if:** <the concrete condition that would make this decision wrong. Scale threshold, new requirement, dependency change.>
